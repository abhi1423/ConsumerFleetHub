spring:
  application:
    name: CONSUMER-SERVICES
  datasource:
    url: jdbc:mysql://localhost:3306/ConsumerServices?createDatabaseIfNotExist=true
    username: root
    password: Abhinav1423
  jpa:
    hibernate.ddl-auto: update
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8086

resilience4j:
  circuitbreaker:
    instances:
      fleetHubCB:
        sliding-window-size: 10
        wait-duration-in-open-state: 10s
        failure-rate-threshold: 50

